services:
  - type: web
    name: jj-multiplayer
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm run start:server
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
